import pygame

COLOR = (0,0,0)

def update_screen(screen, alive):
    screen.fill((230, 230, 230))
    for position in alive:
        rect = pygame.Rect(10 * position[0] + 505, -10 * position[1] + 505, 8, 8)
        pygame.draw.rect(screen, COLOR, rect)
    pygame.display.flip()
